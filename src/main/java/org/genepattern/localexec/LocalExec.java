package org.genepattern.localexec;

import org.genepattern.gpunit.ModuleTestObject;

/**
 * Helper class for executing GP modules on a local workstation, 
 * rather than as part of a GenePattern Server.
 * 
 * @author pcarr
 */
public class LocalExec {
    /**
     * Run the job locally, creating a new working directory.
     * 
     * @param test
     * @return
     */
    static public void runJobLocal(ModuleTestObject test) {
    }
}
