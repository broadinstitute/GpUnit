name: PositiveDirectory
description: Basic test to diff two identical text directories
module: FileDiff
params:
    cmd: diff
    input.filename.1: <%gpunit.testData%>/gpunit/FileDifference/input/
    input.filename.2: <%gpunit.testData%>/gpunit/FileDifference/input/
assertions:
    jobStatus: success

